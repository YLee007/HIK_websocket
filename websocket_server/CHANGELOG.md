# 更新日志

## [v1.1.0] - 2025-11-15

### ✨ 新增功能

#### Ubuntu/Linux系统支持
- 新增Ubuntu系统安装和运行脚本
- 自动化依赖安装
- 完整的Linux平台适配

**新增脚本：**
- `setup_dependencies.sh` - 自动安装依赖
- `build.sh` - 编译脚本
- `run_server.sh` - 运行脚本
- `install_opencv.sh` - OpenCV安装脚本

**新增文档：**
- `UBUNTU_GUIDE.md` - Ubuntu完整安装指南

**支持的Linux发行版：**
- Ubuntu 18.04 LTS
- Ubuntu 20.04 LTS
- Ubuntu 22.04 LTS
- 其他基于Debian的发行版

#### 准星标记功能
- 在图像中心添加可自定义的准星标记
- 支持运行时动态调整准星样式
- 提供完整的API控制接口

**新增API：**
```cpp
// 构造函数新增参数
CameraWebSocketServer(int port, int quality, int fps, bool crosshair = true);

// 准星控制方法
void set_crosshair_enabled(bool enabled);
void set_crosshair_color(int r, int g, int b);
void set_crosshair_size(int size);
void set_crosshair_thickness(int thickness);
```

**准星特性：**
- ✅ 十字线 + 中心圆点 + 外圈圆环
- ✅ 可自定义颜色（RGB）
- ✅ 可调节大小（像素）
- ✅ 可调节线条粗细
- ✅ 运行时开关
- ✅ 默认绿色，大小30，粗细2
- ✅ 性能影响 < 1ms

**使用示例：**
```cpp
CameraWebSocketServer server(9002, 85, 30, true);
server.set_crosshair_color(255, 0, 0);  // 红色
server.set_crosshair_size(40);           // 较大
server.set_crosshair_thickness(3);       // 较粗
server.start();
```

**命令行支持：**
```bash
camera_server.exe 9002 85 30 1  # 启用准星
camera_server.exe 9002 85 30 0  # 禁用准星
```

### 📝 文档更新
- 新增 `CROSSHAIR_GUIDE.md` - 准星功能完整指南
- 新增 `example_crosshair.cpp` - 7个准星使用示例
- 更新 `README.md` - 添加准星功能说明
- 更新 `main.cpp` - 支持准星命令行参数

### 🔧 技术实现
- 在 `camera_websocket_server.hpp` 中添加准星相关成员变量
- 在 `camera_websocket_server.cpp` 中实现 `draw_crosshair()` 函数
- 使用 OpenCV 绘图函数（line, circle）
- 抗锯齿渲染（cv::LINE_AA）

---

## [v1.0.0] - 2025-11-15

### 🎉 初始版本

#### 核心功能
- ✅ 海康相机多线程图像采集
- ✅ WebSocket实时视频流传输
- ✅ JPEG压缩编码
- ✅ 多客户端支持
- ✅ 可调节质量和帧率

#### 客户端
- ✅ 桌面版Web客户端（web_client.html）
- ✅ 移动端优化客户端（mobile_client.html）
- ✅ 实时FPS和延迟显示
- ✅ 响应式设计

#### 构建工具
- ✅ 自动依赖下载脚本（setup_dependencies.bat）
- ✅ 一键编译脚本（build.bat）
- ✅ 一键运行脚本（run_server.bat）
- ✅ CMake构建配置

#### 文档
- ✅ README.md - 完整使用文档
- ✅ QUICKSTART.md - 快速入门指南
- ✅ PROJECT_SUMMARY.md - 项目总结
- ✅ example_usage.cpp - 使用示例

#### 技术栈
- C++14
- WebSocket++ (仅头文件)
- Asio (异步I/O)
- OpenCV (图像处理)
- HIK MVS SDK (相机驱动)

---

## 版本规范

版本号格式：`主版本.次版本.修订版本`

- **主版本**：不兼容的API修改
- **次版本**：向下兼容的功能性新增
- **修订版本**：向下兼容的问题修正

## 未来计划

### v1.2.0 (计划中)
- [ ] 多种准星样式（圆形、方形、三角形等）
- [ ] 准星位置自定义
- [ ] 准星动画效果

### v2.0.0 (计划中)
- [ ] WSS加密传输
- [ ] 用户认证系统
- [ ] H.264视频编码
- [ ] 录像功能
- [ ] 多相机支持

### v3.0.0 (远期规划)
- [ ] 云台控制
- [ ] 移动侦测
- [ ] AI分析集成
- [ ] 云端存储
